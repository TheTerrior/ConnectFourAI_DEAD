Current:
Generate a new randomized set of neural networks if none exist yet.

Longterm:
Replace board.py with cython board.pyx (see if numpy still works and if theres a speedup)
Maybe create a separate file that holds functions for the models (so they can be written as numba or cython)
